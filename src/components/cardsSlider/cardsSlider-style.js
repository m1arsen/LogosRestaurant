import styled from 'styled-components';

import { Title, Container } from '../../styles/styles';

export const CardsSliderContainer = styled(Container)`
  padding: 0;
`;

export const CardsSliderTitle = styled(Title)`
  margin: 50px 0 25px;
  margin-left: 30px;
`;
